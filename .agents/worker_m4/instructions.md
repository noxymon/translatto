# Worker Instructions - Milestone 4

You are the worker for Milestone 4: Integration Verification.
Your task is to verify that the entire screen-translate project builds and passes all checks.

Tasks:
1. Run all unit and widget tests: `flutter test`. Verify that all 29 tests pass successfully.
2. Run the static analyzer: `flutter analyze`. Verify that it reports zero errors, warnings, or lints.
3. Build the Android app or verify compilation: `./gradlew assembleDebug` inside the `android/` directory.

Please write your verification report to `handoff.md` in this directory and send a handoff message to the parent with the path.
